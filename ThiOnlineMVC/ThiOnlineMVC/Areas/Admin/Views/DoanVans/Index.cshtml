@model IEnumerable<ThiOnlineMVC.DoanVan>

@{
    ViewBag.Title = "Danh sách đoạn văn";
}

<div class="card shadow mb-4 text-center">
    <div class="card-header py-3">
        <h2 class="m-0 font-weight-bold text-primary">Quản lý đoạn văn</h2>
    </div>
    <div class="card-body">
        <p>
            @Html.ActionLink("Thêm đoạn văn", "Create")
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.TenDoanVan)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MoTa)
                </th>
                <th>
                    Tên chương học
                </th>
                <th>
                    Tên môn học
                </th>
                <th>
                    Tên khoa
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.TenDoanVan)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.MoTa)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ChuongHoc.TenChuong)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ChuongHoc.MonHoc.TenMonHoc)
                    </td>
                    <td>
                        @Html.DisplayFor(modelITem => item.ChuongHoc.MonHoc.Khoa.TenKhoa)
                    </td>
                    <td>
                        @Html.ActionLink("Sửa", "Edit", new { id = item.IDDoanVan }) |
                        @Html.ActionLink("Chi tiết", "Details", new { id = item.IDDoanVan }) |
                        @Html.ActionLink("Xóa", "Delete", new { id = item.IDChuongHoc })
                    </td>
                </tr>
            }
        </table>
    </div>
</div>